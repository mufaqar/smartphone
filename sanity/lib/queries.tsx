export const blogs = `*[ _type == "blogs"]{
    title,
          slug,
          category,
          mainImage,
          content
        }`;

        export const services = `*[ _type == "services"]{
            title,
                  slug,
                  mainImage,
                  icon,
                  content
                }`;   